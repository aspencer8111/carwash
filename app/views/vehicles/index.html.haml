%h1 Listing vehicles

%table
  %thead
    %tr
      %th Plate
      %th Style
      %th Mud in bed
      %th Bed up
      %th
      %th
      %th

  %tbody
    - @vehicles.each do |vehicle|
      %tr
        %td= vehicle.plate
        %td= vehicle.style
        %td= vehicle.mud_in_bed
        %td= vehicle.bed_up
        %td= link_to 'Show', vehicle
        %td= link_to 'Edit', edit_vehicle_path(vehicle)
        %td= link_to 'Destroy', vehicle, :method => :delete, id: 'destroy'

%br

= link_to 'New vehicle', new_vehicle_path
